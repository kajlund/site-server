meta {
  name: Login Faulty email
  type: http
  seq: 8
}

post {
  url: {{BASE_URL}}/api/v1/users/login
  body: json
  auth: none
}

body:json {
  {
    "email": "ka.lund@gmail.com",
    "password": "Test1234"
  }
}

assert {
  res.status: eq 401
  res.body.success: eq false
  res.body.statusCode: eq 401
  res.body.message: eq Unauthorized
  res.body.detail: 
}

script:pre-request {
  
  
}

tests {
  const body = res.getBody();
  
  test("Result shoud be an object with proper keys",() => {
    expect(body).to.be.an('object').that.has.all.keys('success', 'statusCode', 'message', 'detail');
  });
  
  
}
