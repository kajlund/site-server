meta {
  name: List using limit and skip
  type: http
  seq: 4
}

get {
  url: {{BASE_URL}}/api/v1/tags?limit=10&skip=3
  body: none
  auth: none
}

query {
  limit: 10
  skip: 3
}

assert {
  res.body.data.length: eq 10
  res.body.query.limit: eq 10
  res.body.query.skip: eq 3
  res.body.data[0].tag: eq api
}
