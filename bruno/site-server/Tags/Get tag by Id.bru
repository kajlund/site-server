meta {
  name: Get tag by Id
  type: http
  seq: 5
}

get {
  url: {{URL}}/api/v1/tags/{{tagId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.message: eq Found tag audio
  res.body.data.id: eq {{tagId}}
  res.body.data.tag: eq audio
}
