meta {
  name: List sorted tags
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/api/v1/tags?sort=createdAt
  body: none
  auth: none
}

query {
  sort: createdAt
}

assert {
  res.body.query.sort: eq createdAt
  res.body.data[0].tag: eq analytics
}
