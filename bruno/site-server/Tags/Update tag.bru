meta {
  name: Update tag
  type: http
  seq: 7
}

patch {
  url: {{BASE_URL}}/api/v1/tags/{{addedTagId}}
  body: json
  auth: none
}

body:json {
  {
    "tag": "testtagupdated"
  }
}

assert {
  res.status: eq 200
  res.body.data.tag: eq testtagupdated
  res.body.data.id: eq {{addedTagId}}
}
