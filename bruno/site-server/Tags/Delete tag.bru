meta {
  name: Delete tag
  type: http
  seq: 8
}

delete {
  url: {{BASE_URL}}/api/v1/tags/{{addedTagId}}
  body: none
  auth: none
}

body:json {
  {
    "tag": "APImod"
  }
}

assert {
  res.status: eq 200
  res.body.message: eq Deleted tag testtagupdated
}
